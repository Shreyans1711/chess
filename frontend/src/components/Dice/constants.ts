/** How long the dice take to roll: the animation plus its staggered start. */
export const ROLL_ANIMATION_MS = 900;

/** How often a rolling die flips to another face. */
export const FACE_CHANGE_MS = 80;
